CAPI=2:

name: ::svpwm
description: Space Vector Pulse Width Modulation VHDL core
filesets:
  rtl:
    files:
      - src/svpwm.vhd

  testbench:
    files:
      - test/svpwm.vhd

targets:
  default:
    filesets: [rtl]

  test:
    filesets: [rtl, testbench]
